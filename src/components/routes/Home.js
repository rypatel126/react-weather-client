import React from 'react'
import Layout from '../shared/Layout'

const Home = () => (
  <Layout>
    <h4>You can check the weather with this app!</h4>
    <p>To get started, please sign in and then add a city to view its weather data</p>
  </Layout>
)

export default Home
